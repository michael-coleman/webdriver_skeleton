/**
 * @fileoverview This file is simply to provide bootstrap code for running a
 * command line webdriver instance in the "tinker" style.
 *
 * Usage:
 * 
 * This code will need to be minified with something like 
 * 
 *     npm install --no-save minifier
 *  
 * and
 *  
 *    % ./node_modules/.bin/minify  tinker.js
 *
 * then 
 *
 * * copy the contents of the newly minified file: tinker.min.js
 * * launch node REPL
 * * paste at REPL prompt
 */
function scroll_to_bottom(){var e=Math.max(document.body.offsetHeight,document.documentElement.offsetHeight),n=e-window.innerHeight,t=arguments[arguments.length-1],r=0
!function e(){window.scrollBy(0,1),r+=1,r<n?setTimeout(e,4):(console.log("scrolls_done is: "+r),t("done scrolling, whats next? "))}()}var webdriver=require("selenium-webdriver"),driver;(new webdriver.Builder).forBrowser("chrome").build().then(function(e){driver=e}).then(function(){return driver.manage().window().setSize(683,750)}).then(function(){return driver.manage().timeouts().setScriptTimeout(3e5)})
